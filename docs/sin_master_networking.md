# sin_master module

## Functions

### start(Options) -> any()

Types:

 - Options = [master_option()]

The following options are available:

 - {enabled, boolean()}
 - {interface_ip, string()}
 - {slaves_port, inet:port_number()}
